<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:name=".CuidadoPetApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.CuidadoPet">
        <!-- BroadcastReceiver que recebe os alarmes de medicamento e exibe a notificação -->
        <!-- exported=false: só o próprio app pode disparar este receiver -->
        <receiver
            android:name=".notification.MedicationAlarmReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver que recebe os alarmes de refeição e exibe o lembrete -->
        <receiver
            android:name=".notification.MealAlarmReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver que recebe os alarmes de aniversário e exibe a notificação -->
        <receiver
            android:name=".notification.BirthdayAlarmReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver que processa o botão "Administrado" na notificação de medicamento -->
        <receiver
            android:name=".notification.MedicationAdminReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver que processa o snooze do Super Lembrete -->
        <receiver
            android:name=".notification.SuperReminderSnoozeReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver que processa o snooze das notificações push padrão -->
        <receiver
            android:name=".notification.PushSnoozeReceiver"
            android:exported="false" />

        <!-- Activity de tela cheia do Super Lembrete — aparece sobre a tela de bloqueio -->
        <activity
            android:name=".notification.SuperReminderActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.CuidadoPet.Transparent" />

        <!-- Widget: mostra medicamentos ativos na tela inicial do Android -->
        <receiver
            android:name=".widget.TodayWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_widget_info" />
        </receiver>

        <!-- BootReceiver: reagenda todos os alarmes após o celular reiniciar.
             exported=true é obrigatório para o sistema Android poder disparar o BOOT_COMPLETED.
             Sem isso, os lembretes de medicamento e refeição somem após reinicialização. -->
        <receiver
            android:name=".notification.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- FileProvider: permite compartilhar PDFs gerados pelo app com outros apps (WhatsApp,
             e-mail etc.) sem tornar os arquivos publicamente acessíveis no armazenamento.
             authorities segue convenção <applicationId>.fileprovider
             grantUriPermissions=true: concede acesso temporário ao app receptor -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2305929806870072~2887455158" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CuidadoPet.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>