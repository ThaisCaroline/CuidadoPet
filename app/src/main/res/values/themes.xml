<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema principal do app — sem ActionBar (usamos TopAppBar do Compose) -->
    <style name="Theme.CuidadoPet" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Tema da SuperReminderActivity — fundo transparente para o overlay funcionar -->
    <style name="Theme.CuidadoPet.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>

    <!-- Tema da Splash Screen — exibido pelo sistema enquanto o app está iniciando.
         postSplashScreenTheme: tema aplicado logo depois que a splash desaparece.
         windowSplashScreenBackground: cor de fundo da splash (teal do app).
         windowSplashScreenAnimatedIcon: ícone centralizado na splash (ícone do Canva). -->
    <style name="Theme.CuidadoPet.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.CuidadoPet</item>
    </style>

</resources>
